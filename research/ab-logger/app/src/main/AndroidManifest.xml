<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Intentionally NO storage permissions: same reasoning as every other probe
        in this repo. The CSV copy to /sdcard is written by the root shell itself
        (cat privatefile > /sdcard/...) via xsu, bypassing scoped storage since it
        runs as uid=0. The app process never touches /sdcard through its own file APIs.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        Required since API 30 package visibility filtering: without this,
        PackageManager.getPackageInfo("com.kei.pulse", ...) throws
        NameNotFoundException even when pulse-for-aya IS installed (targetSdk 34
        here is well above the filtering threshold). Confirmed live: pulse_installed
        read false in every session including ones where pulse_service_running (a
        root/dumpsys check, not subject to this filtering) correctly read true.
    -->
    <queries>
        <package android:name="com.kei.pulse" />
    </queries>

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@style/Theme.AbLogger">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".LoggerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-started background telemetry logging (FPS/CPU/GPU/thermal/battery) for A/B device testing" />
        </service>
    </application>

</manifest>
