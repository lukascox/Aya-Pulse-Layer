<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Intentionally NO storage permissions, same reasoning as the other probes in
        this repo: result/CSV copies to /sdcard are written by the root shell itself
        (cat privatefile > /sdcard/...), bypassing scoped storage since it runs as
        uid=0. The app process never touches /sdcard through its own file APIs.
    -->

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@style/Theme.AutoTdpHarness">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
