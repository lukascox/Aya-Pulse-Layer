# Gradle
.gradle/
build/
*/build/
!gradle/wrapper/gradle-wrapper.jar

# Per-developer machine config -- never shared
local.properties

# Android Studio / IntelliJ
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/

# Signing -- real keys/passwords never committed, not even throwaway ones
*.jks
*.keystore
keystore.properties

# Build output / misc
*.apk
*.aab
.DS_Store
# NOTE: *.log was blanket-ignored here before 2026-07-25 -- silently excluded real,
# valuable historical diagnostic logs under docs/archive/pulse_lite/ and
# diagnostics/ (confirmed: every *.log in this repo today is one of those, none
# are throwaway build/session byproducts). Removed rather than scoped-excepted --
# re-add it scoped to a specific throwaway path if one ever needs it.

# Upstream reference clones (separate git history, GPL-2.0 source we read but don't
# fork into this repo directly) -- keep local only, never as a submodule/gitlink.
research/pulse-upstream/
research/konabess-g3gen3-upstream/
research/clustertune-upstream/
research/pam-stock-os-optimization-upstream/
